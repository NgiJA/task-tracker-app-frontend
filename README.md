Task Tracker App is a project based on a to-do list app, but with additional features added, as follows:
-User Authentication (login and register)
-Reset password (verify OTP via email)
-Google login
-Excel report
-PDF report
